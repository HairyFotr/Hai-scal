program HelloWorld;

const 
    aaa := ^50;
    bbb := not false;
    ccc := 1 + 1 + 1;
    ddd := lol.neki div lol.nekineki;

type
    ccc = integer;
    ddd = char;
    
var
    eee:integer;
    fff:boolean;
    
procedure(); begin end;
function():boolean; begin end;
function(i:integer; b:boolean):integer; begin end;
function(i:integer; b:boolean;):integer; begin end;
procedure(c:char;); begin end;

begin
    2+2;
    4*3;
    for i := 0 to 10 do write('x');
    if(i = 10+1) then write('y');
    1+1;
end.
